#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a = 97;
	int b = 65;

	while (a <= 122)
	{
		putchar(a);
		n++;
	}
	while (b <= 98)
	{
		putchar(b);
		n++;
	}
	putchar('\n');
	return (0);
}
